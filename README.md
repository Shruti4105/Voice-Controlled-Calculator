# Voice-Controlled Calculator

A Python app that listens to your voice commands, parses simple arithmetic expressions, and prints the result.

## Requirements

- Python 3.x
- Microphone access
- Install dependencies:

```bash
pip install -r requirements.txt